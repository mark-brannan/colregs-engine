# Conformance findings register

Findings from `npm run conformance` (research/conformance/), the exhaustive
predicate-level check for [issue #6](https://github.com/mark-brannan/colregs-engine/issues/6)
(Phase 0 of [issue #1](https://github.com/mark-brannan/colregs-engine/issues/1)).

Status ladder: **candidate** (found by the harness, unreviewed) ->
**agent-verified** (a second agent pass confirmed it's real and not a
harness bug) -> **triaged** (an agent classified it and wrote the reason,
including "harness false positive"; a person has not yet ruled) ->
**human-reviewed** (a person ruled: data bug, genuine ambiguity, engine
bug, or harness bug) -> **landed** (fixed as a fixture, an ADR, or a
requirement change). A finding may skip agent-verified and go straight to
triaged when the agent's read is that the harness, not the data, is wrong.

Agents never edit colregs normatively; these are candidates for a human to
triage, not fixes.

Every column but the last two is regenerated by the run. **status** and
**triage note** come from [`triage.json`](triage.json), a hand-maintained
sidecar keyed by `check::groupKey` (not by `FIND-nn`, which is just a
position in sorted order and shifts as findings appear or disappear) --
climb a finding up the ladder by editing that file and rerunning
`npm run conformance`, not by hand-editing this table, which the next run
overwrites.

| id | check | records | description | cites | status | triage note |
|---|---|---|---|---|---|---|
| FIND-01 | conformance-encounter-roles | 2232 | the engine reads one frame and never sees ["rule:12a_i"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 12(a)(i) | candidate | - |
| FIND-02 | conformance-encounter-roles | 341 | the engine reads one frame and never sees ["rule:12a_i","rule:18d_i"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 12(a)(i); 18(d)(i) | candidate | - |
| FIND-03 | conformance-encounter-roles | 124 | the engine reads one frame and never sees ["rule:12a_i","rule:18d_i","rule:9c"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 12(a)(i); 18(d)(i); 9(c) | candidate | - |
| FIND-04 | conformance-encounter-roles | 930 | the engine reads one frame and never sees ["rule:12a_i","rule:18f_i"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 12(a)(i); 18(f)(i) | candidate | - |
| FIND-05 | conformance-encounter-roles | 744 | the engine reads one frame and never sees ["rule:12a_i","rule:9c"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 12(a)(i); 9(c) | candidate | - |
| FIND-06 | conformance-encounter-roles | 248 | the engine reads one frame and never sees ["rule:12a_ii","rule:9c"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 12(a)(ii); 9(c) | candidate | - |
| FIND-07 | conformance-encounter-roles | 124 | the engine reads one frame and never sees ["rule:12a_iii","rule:9c"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 12(a)(iii); 9(c) | candidate | - |
| FIND-08 | conformance-encounter-roles | 426912 | the engine reads one frame and never sees ["rule:13a"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 13(a) | candidate | - |
| FIND-09 | conformance-encounter-roles | 65664 | the engine reads one frame and never sees ["rule:13a","rule:9c"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 13(a); 9(c) | candidate | - |
| FIND-10 | conformance-encounter-roles | 270 | the engine reads one frame and never sees ["rule:15a:keep_out_of_the_way"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 15(a) | candidate | - |
| FIND-11 | conformance-encounter-roles | 234 | the engine reads one frame and never sees ["rule:15a:keep_out_of_the_way","rule:18d_i"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 15(a); 18(d)(i) | candidate | - |
| FIND-12 | conformance-encounter-roles | 162 | the engine reads one frame and never sees ["rule:15a:keep_out_of_the_way","rule:18d_i","rule:9c"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 15(a); 18(d)(i); 9(c) | candidate | - |
| FIND-13 | conformance-encounter-roles | 162 | the engine reads one frame and never sees ["rule:15a:keep_out_of_the_way","rule:9c"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 15(a); 9(c) | candidate | - |
| FIND-14 | conformance-encounter-roles | 2604 | the engine reads one frame and never sees ["rule:18a_i"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 18(a)(i) | candidate | - |
| FIND-15 | conformance-encounter-roles | 372 | the engine reads one frame and never sees ["rule:18a_iii"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 18(a)(iii) | candidate | - |
| FIND-16 | conformance-encounter-roles | 372 | the engine reads one frame and never sees ["rule:18a_iv"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 18(a)(iv) | candidate | - |
| FIND-17 | conformance-encounter-roles | 2511 | the engine reads one frame and never sees ["rule:18b_i"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 18(b)(i) | candidate | - |
| FIND-18 | conformance-encounter-roles | 1054 | the engine reads one frame and never sees ["rule:18b_iii"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 18(b)(iii) | candidate | - |
| FIND-19 | conformance-encounter-roles | 9314 | the engine reads one frame and never sees ["rule:18d_i"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 18(d)(i) | candidate | - |
| FIND-20 | conformance-encounter-roles | 4178 | the engine reads one frame and never sees ["rule:18d_i","rule:9c"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 18(d)(i); 9(c) | candidate | - |
| FIND-21 | conformance-encounter-roles | 26784 | the engine reads one frame and never sees ["rule:18f_i"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 18(f)(i) | candidate | - |
| FIND-22 | conformance-encounter-roles | 51714 | the engine reads one frame and never sees ["rule:9c"] fire for the other vessel, so pooling both adds the roles those entries lay and displaces what they override | 9(c) | candidate | - |
| FIND-23 | consistency-both-give-way | 241157 | both vessels are give-way at once: self by rule:13a, the other by rule:13a, neither overridden | 13(a); 13(a) | candidate | - |
| FIND-24 | consistency-both-give-way | 414 | both vessels are give-way at once: self by rule:15a:keep_out_of_the_way, the other by rule:15a:keep_out_of_the_way, neither overridden | 15(a); 15(a) | candidate | - |
| FIND-25 | consistency-encounter-unresolved | 12 | the encounter is classified encounter:head-on with risk of collision asserted, but no precedence entry lays a role on either vessel | - | candidate | - |
