# Codex review of proposal v4 — 2026-09-04 (no-hedging terms)

Reviewer: OpenAI codex CLI 0.153.4, headless, model not recorded. Prompt required a paste-ready fix per finding. Verbatim output.

1. **Critical — verified-from-source.** Target: “`care | 2(a), 5, 7(a) | anything | factors only | no | not verifiable`” and “the engine … never evaluates `care`.” Rule 5 and Rule 7(a) each say “shall”; they are not merely factors. Rule 7(d) is correctly described. [Official UK rule extract](https://assets.publishing.service.gov.uk/media/547c6faa40f0b60241000049/HomelandScottishVikingAnnexes.pdf)

   Replace the `care` row with:

   ```text
   | `care` | 2(a) | anything | residual responsibility / precaution constraint | no | represented, never independently evaluated |
   ```

   Replace the `conduct` membership with:

   ```text
   | `conduct` | 5, 6, 7(a)–(c), 8(a)–(e), 9(a),(e)–(g), 10(a)–(h),(k)–(l), 16, 17, 19(b)–(e) | encounter + role + phase + kinematics + observation record | obligated or prohibited action | as a monitor over a trace | TLA+, STL |
   ```

   Replace the second ink sentence with:

   ```text
   **Ink:** the engine evaluates `scope`, `display`, `classification`, and `precedence`; monitors `conduct`; represents but does not independently evaluate `care`.
   ```

2. **Critical — verified-from-source plus modelling expertise.** Target: “R1 is the Rule 2 region proper.” Rule 2(b) says special circumstances *may make a departure necessary to avoid immediate danger*; it does not define every model state lacking a compliant robust policy as Rule-2 territory. Also, `∀ A` quantifies over an action set, not the other vessel’s strategies. [Official UK rule extract](https://assets.publishing.service.gov.uk/media/547c6faa40f0b60241000049/HomelandScottishVikingAnnexes.pdf)

   Replace the ontology table and following two paragraphs through “R2 … model found none” with:

   ```text
   **Ink, the ontology.** Let `Safe(s, σown, σother)` mean no level-1/2 violation within T. Let `A` be the declared set of admissible other-vessel strategies. For a situation s:

   | region | definition | meaning |
   |---|---|---|
   | **R0 rules-suffice** | ∃ compliant σown ∀ σother ∈ A: Safe(s, σown, σother) | a compliant robust policy exists in this model |
   | **R1 departure-required-in-model** | ¬R0 ∧ ∃ σown ∀ σother ∈ A: Safe(s, σown, σother) | every robust safe policy breaches at least one Rules 4–19 constraint |
   | **R2 unwinnable-in-model** | ¬∃ σown ∀ σother ∈ A: Safe(s, σown, σother) | no robust safe policy exists in this model |

   “Compliant” means satisfying the encoded Rules 4–19 constraints; Rule 2 is not included in that predicate. R1 is a model finding that supplies evidence relevant to Rule 2(b), not a legal classification. An R1 advisory identifies its breached paragraphs; R2 reports that no robust escape was certified.
   ```

3. **Major — expertise.** Target: “When `s` is in R1 or R2 it returns … `inconclusive-in-model`.” Inconclusive is not region membership.

   Replace that sentence with:

   ```text
   The grid query returns exactly one certified result: R1, R2, R0/not-flagged, or `inconclusive-in-model`; only certified R1 and R2 produce the Rule 2 banner.
   ```

**Verdict: approve with the changes above.**